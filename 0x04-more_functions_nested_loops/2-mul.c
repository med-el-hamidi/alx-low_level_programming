#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: variable of type int
 * @b: variable of type int
 * Return: Always mul(a,b).
 */
int mul(int a, int b)
{
	return (a * b);
}
